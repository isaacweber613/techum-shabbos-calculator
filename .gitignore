.claude/settings.local.json

# Local analytics store (may contain searched addresses / pins)
data/
dist/
.wrangler/
.dev.vars*
node_modules/
worker-configuration.d.ts
worker-startup.cpuprofile
.codex-remote-attachments/
tmp/

# Scratch / agent notes — not product source
grok review.md
