{
  "name": "techum-shabbos-calculator",
  "version": "1.5.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node tests/geometry.test.js && node tests/app.test.js && node tests/seo.test.js && node tests/projection-performance.test.js && node tests/woodbourne-benchmark.test.js && node tests/registry.test.ts && node tests/buildings-cache.test.ts && node tests/map-config.test.ts",
    "typecheck": "wrangler types worker-configuration.d.ts && tsc --noEmit",
    "cf:types": "wrangler types worker-configuration.d.ts",
    "cf:dev": "npm run build && wrangler dev --local",
    "cf:dry-run": "npm run build && wrangler deploy --dry-run",
    "cf:deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "latest"
  },
  "dependencies": {
    "@mapbox/vector-tile": "^3.0.0",
    "pbf": "^5.1.2",
    "pmtiles": "^4.4.1"
  }
}
